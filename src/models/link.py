"""
    src / models / link.py
    ----------------------
    Neutral network model that is being trained on the distance vector between
    the transmitter (UAV) and receiver (antenna) along with the `rx_type` that
    can be either `terrestrial` (ground-based) or `air-bound`
"""